use database ibrd_project;
USE SCHEMA IBRD_SILVER;

CREATE OR REPLACE STREAM IBRD_LOANS_RAW_STREAM
ON TABLE IBRD_PROJECT.IBRD.IBRD_LOANS_RAW
APPEND_ONLY = TRUE;


CREATE OR REPLACE TASK IBRD_PROJECT.IBRD.REFRESH_SILVER_MASTER_TASK
  WAREHOUSE = COMPUTE_WH
  SCHEDULE = 'USING CRON * * * * * UTC'
  WHEN SYSTEM$STREAM_HAS_DATA('IBRD_PROJECT.IBRD.IBRD_LOANS_RAW_STREAM')
AS
CALL SYSTEM$RUN_TASK('dbt_run_stage_and_silver');